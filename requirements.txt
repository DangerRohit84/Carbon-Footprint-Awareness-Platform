flask==3.0.0          # Web framework
python-dotenv==1.0.0  # Load .env variables into os.environ
pytest==8.0.0         # Test runner (dev dependency)
gunicorn==21.2.0      # Production WSGI server (used in Dockerfile)
